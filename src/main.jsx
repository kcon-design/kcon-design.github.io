import React from 'react';
import { createRoot } from 'react-dom/client';
import KconPortfolio from './KconPortfolio.jsx';

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <KconPortfolio />
  </React.StrictMode>
);
